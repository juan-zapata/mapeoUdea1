<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="medellin.mapa.google">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PopupEconomicasyFinancieras"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup"></activity>
        <activity
            android:name=".PopupFarmaceuticasyAlimentarias"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupArtes"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupBloque9"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupComunicaciones"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupExactasyNaturales"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupDerecho"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupIdiomas"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupMuseo"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupTeatro"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupBiblioteca"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupBienestar"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupAdministrativo"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".PopupIngenieria"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.TemaPopup" />
        <activity
            android:name=".MapaCiu"
            android:screenOrientation="fullSensor" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".p"
            android:label="@string/title_activity_p" />
        <activity
            android:name=".MapsActivityUbicacion"
            android:label="@string/title_activity_maps_ubicacion" />
        <activity
            android:name=".MapsActivity1"
            android:label="@string/title_activity_maps1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>